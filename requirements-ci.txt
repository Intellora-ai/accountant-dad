# CI tooling, pinned exactly.
#
# Unpinned tools make a gate non-deterministic: the same commit can pass today
# and fail tomorrow because a linter added a rule. Every version here is the one
# the gates were proven against.
#
# This file is itself scanned by the `dependency scan` gate.

build==1.3.0
coverage==7.11.0
mypy==1.18.2
pip-audit==2.9.0
pytest==8.4.2
pytest-randomly==4.0.1
PyYAML==6.0.2
ruff==0.14.5
twine==6.2.0
types-PyYAML==6.0.12.20250915
