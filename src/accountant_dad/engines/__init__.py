"""The six engines. One package each, and no arrow between any two of them.

`APPLICATION_LAYER_INVARIANTS.md` AL-INV-5 — *"Engines never call each other.
Every artifact passes through the Application Layer."* P3 ships a stub per
engine (`MVP_IMPLEMENTATION_BLUEPRINT.md:100`); the reasoning arrives at P4.
"""
