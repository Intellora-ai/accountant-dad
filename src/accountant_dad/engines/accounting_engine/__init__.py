"""The Accounting Engine. `ENGINE_3_ACCOUNTING_ENGINE_RULES.md`, locked.

*"Given what happened in the business, how should this be represented according
to accounting rules?"* (`:28`.) It produces exactly one artifact, the Accounting
Decision (`:164`), out of exactly nine sub-engines (`:289`).

P3 ships a stub and nothing else. `MVP_IMPLEMENTATION_BLUEPRINT.md:100`
schedules engine stubs 1-6 here; `:136` permits **no accuracy claim at this
phase**. The nine sub-engines, the accounting reasoning and the tax reasoning
arrive at P4 and are frozen until then by `CLAUDE.md` §P.
"""
