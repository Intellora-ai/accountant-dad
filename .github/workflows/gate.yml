name: gate

permissions:
  contents: read

on:
  push:
  pull_request:

jobs:
  build:
    name: typecheck · lint · tests · build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: not implemented
        run: |
          echo "MVP_BUILD_VERIFY_FIX.md §2 condition 2 — not implemented"
          exit 1

  conformance:
    name: conformance suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: not implemented
        run: |
          echo "MVP_BUILD_VERIFY_FIX.md §2 condition 3 — not implemented"
          exit 1

  negative-controls:
    name: negative controls 9 of 9
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: not implemented
        run: |
          echo "MVP_BUILD_VERIFY_FIX.md §2 condition 4 — not implemented"
          exit 1
